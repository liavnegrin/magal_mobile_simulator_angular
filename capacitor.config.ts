import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'magal.mobiledemo.angular',
  appName: 'mobile-simulator',
  webDir: 'dist/mobile-simulator',
  bundledWebRuntime: false
};

export default config;
